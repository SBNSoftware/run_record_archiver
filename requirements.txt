# Main application dependencies
pyyaml>=6.0
pytest>=7.0
requests>=2.26.0
psycopg2-binary>=2.9.0  # Required by ucondb for PostgreSQL access
slack-bolt>=1.18.0  # Slack integration for failure notifications

# UconDB client from git repository
git+https://github.com/ivmfnal/ucondb.git#egg=ucondb

# Note: The 'conftoolp' package is a system-level dependency provided by
# the artdaq_database environment. It is not available on PyPI and should
# not be listed here. The application expects the environment to be
# correctly sourced before execution.
